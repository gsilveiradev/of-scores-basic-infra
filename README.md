## OneFootball Scores team basic infrasctructure

This project includes the basic infrastructure for local development, including:
- MySQL
- Redis
- RabbitMQ

Run docker-compose to start the containers:

```shell
docker-compose up -d
```